function prepareLinks(postids) {
    let linkarray = [];
    for(let i=0; i < postids.length; i++) {
        linkarray.push(
            "https://quickhq.tech/"postids[i]
            )
    }
    return []
}

module.exports = {
    
}